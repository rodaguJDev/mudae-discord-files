class MudaeModule {
    constructor(gui) {
        throw new Error("Not Implemented Exception")
    }
}

class MudaeAutoClaim extends MudaeModule {
    constructor(gui) {
        // Remember, extending from MudaeModule means implementing it into the GUI, so that's what super() will do
        super();
    }
}